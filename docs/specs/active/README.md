# Specs Aktif
Direktori ini menyimpan dokumen spesifikasi fitur aktif (`docs/specs/active/<feature-slug>.md`) untuk pekerjaan kompleks sesuai standar Fikran Engineering.
Setelah pekerjaan selesai dan terverifikasi, pindahkan ke `docs/specs/done/`.
